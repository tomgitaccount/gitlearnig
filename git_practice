git init
git add filename/untracked->unmodified(tracked)
edit and save the file/unmodified(tracked)->unstaged (tracked/modified)
git add filename/unstaged (tracked/modified)->staged (tracked/modified)
git commit/staged(tracked/modified)->unmodified
untracked/unmodified/unstaged/staged/unmodified

git add filename/git commit


undo staged files

git branch/show all branch names

git branch branchname/create a new branch

git checkout branchname/choose a branch
git checkout -b branchname/git branch branchename + git checkout branchname

git branch branchname,git merge anotherbranchname
git branch --delete anotherbranchname/delete branch,
git branch -D [branch-name]/delete forcement the branch

git push [remote-name] --delete [branch-name]/ delete the remote branch [remote-name]/[branch-name]

git rebase [branch-name] /rebase the current branch to [branch-name]

